{
  "version": "1.0.0-alpha.4",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
