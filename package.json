{
  "version": "1.0.0-alpha.4",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "eslint": "^7.15.0",
    "lerna": "^2.11.0",
    "prettier-plugin-organize-imports": "^1.1.1"
  }
}
