export {
	clampCanvasOutlineValue as clamp,
	mixCanvasOutlinePoints as mixPoint,
	mixCanvasOutlineValues as mix,
} from '@remotion/canvas';
export type {
	CanvasOutline as SelectedOutline,
	CanvasOutlinePoint as OutlinePoint,
} from '@remotion/canvas';
