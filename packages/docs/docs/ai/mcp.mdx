---
image: /generated/articles-docs-ai-mcp.png
crumb: 'AI'
title: Remotion's Model Context Protocol (deprecated)
sidebar_label: MCP (deprecated)
---

:::warning Deprecated
The Remotion MCP is deprecated. Install [Remotion Agent Skills](/docs/ai/skills) and use `/remotion-docs` instead.
:::

The hosted MCP remains available during a transition period for existing users. New installations are not recommended.

## Migrate to `/remotion-docs`

<Step>1</Step> Remove `remotion-documentation` from the MCP configuration in your editor.

<Step>2</Step> Install the Remotion Agent Skills from your Remotion project:

```bash title="Terminal"
npx remotion skills add
```

<Step>3</Step> Ask your coding agent to use `/remotion-docs` when it needs current Remotion documentation.

Restart the coding agent if it does not detect the newly installed skills.
