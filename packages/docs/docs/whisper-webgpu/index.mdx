---
image: /generated/articles-docs-whisper-webgpu-index.png
title: '@remotion/whisper-webgpu'
crumb: 'Transcribe audio locally'
---

# @remotion/whisper-webgpu<AvailableFrom v="4.0.518" />

Transcribe audio locally in the browser or Node.js using timestamped Whisper models and WebGPU through [Transformers.js](https://huggingface.co/docs/transformers.js), and convert the result to [`@remotion/captions`](/docs/captions).

import {TableOfContents} from './table-of-contents';

## Installation

<Installation pkg="@remotion/whisper-webgpu @huggingface/transformers" />

## Example

```ts twoslash title="transcribe.ts"
import {clearStaleModels, downloadWhisperModel, resampleTo16Khz, toCaptions, transcribe} from '@remotion/whisper-webgpu';

await clearStaleModels();
await downloadWhisperModel({model: 'small.en'});

const file = new File([], 'audio.wav');
const channelWaveform = await resampleTo16Khz({file});

const transcription = await transcribe({
  channelWaveform,
  model: 'small.en',
  language: 'en',
});

const {captions} = toCaptions({whisperWebGpuOutput: transcription});
console.log(captions);
```

[`downloadWhisperModel()`](/docs/whisper-webgpu/download-whisper-model) stores model files in the persistent cache without initializing WebGPU. Call [`loadWhisperModel()`](/docs/whisper-webgpu/load-whisper-model) when you want to initialize the model explicitly.

See [Using `@remotion/whisper-webgpu` in Node.js](/docs/whisper-webgpu/node) for a complete Node.js example.

## Model hosting

We noticed that hosting the models on R2 leads to faster downloading than through Hugging Face. Therefore, we mirrored the models to `remotion.media`, keeping them byte-identical.

## APIs

<TableOfContents />

## Requirements

WebGPU requires HTTPS in production or `localhost` during development. Use [`canUseWhisperWebGpu()`](/docs/whisper-webgpu/can-use-whisper-webgpu) to check for a usable adapter.

## License

MIT
