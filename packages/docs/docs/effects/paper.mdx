---
image: /generated/articles-docs-effects-paper.png
slug: /effects/paper
title: paper()
sidebar_label: paper()
crumb: '@remotion/effects'
---

# paper()<AvailableFrom v="4.0.486" />

_Part of the [`@remotion/effects`](/docs/effects/api) package._

Adds a procedural paper texture to canvas-based components such as [`<Video>`](/docs/media/video), [`<HtmlInCanvas>`](/docs/remotion/html-in-canvas), [`<CanvasImage>`](/docs/canvasimage) and [`<Solid>`](/docs/solid).

The effect adds paper grain, fibers, crumples, folds and speckles while preserving the source alpha mask.

Based on the open-source [Paper Shaders paper texture](https://shaders.paper.design/paper-texture) by Paper Design.

## Preview

<EffectsDemo type="effects-paper" />

## Example

```tsx twoslash title="MyComp.tsx"
import {paper} from '@remotion/effects/paper';
import {AbsoluteFill, Solid} from 'remotion';

export const MyComp: React.FC = () => {
  return (
    <AbsoluteFill>
      <Solid
        width={1280}
        height={720}
        color="#fff4d0"
        effects={[
          paper({
            roughness: 0.52,
            fiber: 0.58,
            crumples: 0.42,
            folds: 0.32,
            foldCount: 9,
            drops: 0.16,
            seed: 21,
          }),
        ]}
      />
    </AbsoluteFill>
  );
};
```

## API

Pass an object with the following properties. You can also call `paper()` without arguments.

### `amount?`

Strength of the paper texture from `0` to `1`. Defaults to `1`.

Use `0` to leave the source unchanged.

### `colorFront?`

Light-facing paper color. Defaults to `#fff8df`.

Alpha in the color controls how strongly this color tints the paper highlights.

### `colorBack?`

Shadow-facing paper color. Defaults to `#b9a375`.

Alpha in the color controls how strongly this color tints the paper shadows.

### `contrast?`

Sharpness of light and color transitions from `0` to `1`. Defaults to `0.7`.

Higher values make folds and crumples more pronounced.

### `roughness?`

Fine pixel noise from `0` to `1`. Defaults to `0.45`.

Use `0` for a smoother texture.

### `fiber?`

Curly fiber intensity from `0` to `1`. Defaults to `0.45`.

Higher values add more visible paper strands.

### `fiberSize?`

Curly fiber scale from `0.01` to `1`. Defaults to `0.55`.

Smaller values create finer fibers. Must be greater than `0`.

### `crumples?`

Cell-based crumple intensity from `0` to `1`. Defaults to `0.35`.

Higher values add stronger small-scale crumple marks.

### `crumpleSize?`

Cell-based crumple scale from `0.01` to `1`. Defaults to `0.5`.

Smaller values create tighter crumple cells. Must be greater than `0`.

### `folds?`

Fold lighting intensity from `0` to `1`. Defaults to `0.25`.

Use `0` to remove the broader fold pattern.

### `foldCount?`

Number of generated folds from `0` to `15`. Defaults to `7`.

Higher values create more fold lines.

### `drops?`

Speckle visibility from `0` to `1`. Defaults to `0.12`.

Use `0` to remove the speckle pattern.

### `fade?`

Large-scale mask applied to the paper pattern from `0` to `1`. Defaults to `0.2`.

Higher values make the paper details less uniform across the frame.

### `seed?`

Seed for folds, crumples and speckles from `0` to `1000`. Defaults to `0`.

Change it to get a different static paper pattern.

### `disabled?`

When `true`, the effect is skipped. Defaults to `false`.

## Requirements

`paper()` uses a WebGL2 backend. During renders, enable WebGL via [`Config.setChromiumOpenGlRenderer('angle')`](/docs/config#setchromiumopenglrenderer). See [Using WebGL during renders](/docs/remotion/html-in-canvas#using-webgl-during-renders).

## See also

- [`@remotion/effects`](/docs/effects/api)
- [`burlap()`](/docs/effects/burlap)
- [`noise()`](/docs/effects/noise)
- [Paper Shaders](https://shaders.paper.design)
- [Source code for this effect](https://github.com/remotion-dev/remotion/blob/main/packages/effects/src/paper.ts)
