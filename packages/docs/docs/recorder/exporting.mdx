# Exporting

import { RecordingButton } from "../../components/recorder/RecordButton";

<div style={{ display: "flex", flexDirection: "row", alignItems: "center" }}>
  <div> The easiest way to export the video is to click the </div>
  <RenderButton /> <div>button.</div>
</div>
The rendered videos get stored in the `/out` folder of the project root.

For other ways to render, see the [Remotion documentation](https://remotion.dev/docs/render)
