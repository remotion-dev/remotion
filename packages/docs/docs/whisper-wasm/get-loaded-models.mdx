---
image: /generated/articles-docs-whisper-wasm-get-loaded-models.png
title: getLoadedModels()
crumb: '@remotion/whisper-wasm'
---

Returns an array of Whisper models that have already been downloaded and stored in the browser's IndexedDB storage.

## Return value

Returns a Promise that resolves to an array of `WhisperWasmModel` strings, representing the models that are already downloaded and available locally.
