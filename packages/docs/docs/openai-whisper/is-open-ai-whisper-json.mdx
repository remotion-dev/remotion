---
image: /generated/articles-docs-openai-whisper-is-open-ai-whisper-json.png
title: isOpenAiWhisperJson()
crumb: '@remotion/openai-whisper'
---

# isOpenAiWhisperJson()<AvailableFrom v="4.0.521" />

Returns whether a value has the top-level fields of an OpenAI Whisper verbose JSON response.

Use it before [`openAiWhisperApiToCaptions()`](/docs/openai-whisper/openai-whisper-api-to-captions) when accepting JSON whose source is unknown.

This function detects the format but does not validate every word. The converter validates the fields it uses and throws an actionable error if the recognized input is malformed.

## Example

```ts twoslash title="convert-upload.ts"
import {
  isOpenAiWhisperJson,
  openAiWhisperApiToCaptions,
} from '@remotion/openai-whisper';

const contents = '{"language":"english","text":"Hello","words":[]}';
const json = JSON.parse(contents);

if (isOpenAiWhisperJson(json)) {
  const {captions} = openAiWhisperApiToCaptions({transcription: json});
  console.log(captions);
}
```

## API

### `value`

The value to inspect. It may have any JavaScript type.

### Return value

A boolean. It is `true` when the value is an object without `language_code`, with `text`, and with at least one of `task`, `language`, `duration`, `segments`, or `words` as top-level fields.

Other additional fields do not affect detection.

## Compatibility

<CompatibilityTable chrome firefox safari nodejs bun serverlessFunctions clientSideRendering serverSideRendering player studio />

## See also

- [`openAiWhisperApiToCaptions()`](/docs/openai-whisper/openai-whisper-api-to-captions)
- [`isElevenLabsTranscriptJson()`](/docs/elevenlabs/is-elevenlabs-transcript-json)
- [Source code for this function](https://github.com/remotion-dev/remotion/blob/main/packages/openai-whisper/src/openai-whisper-api-to-captions.ts)
