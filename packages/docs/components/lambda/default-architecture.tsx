import { DEFAULT_ARCHITECTURE } from "@remotion/lambda/dist/defaults";
import React from "react";

export const DefaultArchitecture: React.FC = () => {
  return <span>{DEFAULT_ARCHITECTURE}</span>;
};
