# Vercel Sandbox OIDC Token
# In development, if not set, the SDK will initiate a device authorization flow.
# In production, set up Vercel OIDC: https://vercel.com/docs/oidc
# VERCEL_OIDC_TOKEN=

# Vercel Blob Storage (for storing rendered videos)
# Create a Blob store at: Vercel Dashboard -> Storage -> Create -> Blob
# BLOB_READ_WRITE_TOKEN=

# URL of your deployed Remotion bundle (optional - if not set, will bundle on-demand)
# REMOTION_SERVE_URL=https://your-remotion-bundle.vercel.app

# Sandbox Snapshot ID (optional - speeds up renders by skipping setup)
# Create a snapshot by running: bun run create-snapshot
# Note: Snapshots expire after 7 days
# SANDBOX_SNAPSHOT_ID=
