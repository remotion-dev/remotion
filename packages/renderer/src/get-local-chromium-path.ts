// import puppeteer from 'puppeteer-core';
// import os, { type } from 'os';

// export const getLocalChromiumPath = async ():Promise<string> => {
//   // const allCurrentChromiumVersions = await fetch('http://omahaproxy.appspot.com/all.json');
//   // const allCurrentChromiumVersionsJson = await allCurrentChromiumVersions.json();
//   // const osPlatform = os.platform() === 'darwin' ? 'mac' : os.platform() === 'win32' ? 'win' : os.platform() === 'linux' : null;
//   // if(typeof osPlatform === null){
//   //   throw new Error("We don't support your OS yet");
//   // }
//   const browserFetcher = puppeteer.;
//   const revisionInfo = await browserFetcher.download('533271');

//   return 'avl';
// }
